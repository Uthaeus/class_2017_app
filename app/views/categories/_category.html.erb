<%= form_for(category) do |f| %>
  
  <div class="form-group">
    <%= f.label :category_id %>
    <%= f.text_field :category_id, class: 'form-control', placeholder: 'Name' %>
  </div>

  <div class="form-group">
    <%= f.submit "Submit", class: 'btn btn-primary' %>
  </div>

<% end %>